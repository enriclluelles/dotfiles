## Jujutsu (jj)

- Use `jj` for version control, not `git`
- Parent syntax: `@-` not `@~1` or `@^`
- Do not use Git terminology in Jujutsu projects
- Do not use wrong jj syntax (~N, ^, interactive flags)
- Do not access `.jj/` or `.git/` directories directly — use commands
